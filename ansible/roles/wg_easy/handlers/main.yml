---
- name: Restart caddy
  community.docker.docker_compose_v2:
    project_src: "{{ wg_easy_dir }}"
    state: restarted
    services:
      - caddy
  become: true
  become_user: "{{ docker_user }}"
