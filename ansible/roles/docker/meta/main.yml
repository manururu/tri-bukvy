---
galaxy_info:
  role_name: docker
  description: Installs Docker Engine (docker-ce) from Docker's official apt repository and adds docker_user to the docker group
  min_ansible_version: "2.15"

dependencies: []
