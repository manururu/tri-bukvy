# Домен и почта подставляются из env контейнера caddy (см. docker-compose.yaml):
# DOMAIN/EMAIL приезжают в .env на сервере из секретов GitHub.
{
	email {$EMAIL}
}

{$DOMAIN} {
	encode gzip
	reverse_proxy app:8080
}
